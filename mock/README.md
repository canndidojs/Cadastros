# Como usar

1. Abra uma aba no terminal
2. Execute o comando conforme o seu instalador de pacotes:

```npx json-server -w -p 3333 ./mock/database.json```

ou

```yarn run json-server -w -p 3333 ./mock/database.json```